name: Hello World
on: [push]
jobs:
  hello-world:
    runs-on: alpine-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Print message
        run: echo "Hello, World!"Copied!   
