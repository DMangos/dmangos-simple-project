name: Hello World
on: [push]
jobs:
  hello-world:
    runs-on: ubuntu-slim
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Print message
        run: echo "Hello, World!"Copied!   
